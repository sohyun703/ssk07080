def solution(arr):
    answer = []
    false =[]
    answer.append(arr[0])
    #연속된 숫자를 제거
    for i in range(1,len(arr)):
        if arr[i-1] == arr[i]:
            false.append(arr[i])
        else:
            answer.append(arr[i])
